name: CI

on:
  push:
    branches:
      - main
    tags:
      - "*"
    paths-ignore:
      - "docs/**"
      - "README.md"
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  HEASARC_CHANNEL: "https://heasarc.gsfc.nasa.gov/FTP/software/conda"

jobs:
  download:
    name: Download Xspec
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_info.outputs.version }}
      build: ${{ steps.get_info.outputs.build }}
      fn: ${{ steps.get_info.outputs.fn }}
      url: ${{ steps.get_info.outputs.url }}
      data-fn: ${{ steps.get_info.outputs.data-fn }}
      data-url: ${{ steps.get_info.outputs.data-url }}
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - name: Install Miniforge
        uses: conda-incubator/setup-miniconda@v3
        with:
          miniforge-version: latest

      - name: Install Utils
        run: |
          sudo apt-get install -y aria2 jq

      - name: Get Latest Version of Xspec
        id: get_info
        run: |
          get_latest_pkg() {
            jq '
              .result.pkgs
              | sort_by(
                  (
                    .version | split(".") | map(tonumber) + [0, 0]
                  )[0:3]
                  + [.build_number]
                )
              | .[-1]
            '
          }

          xspec=$(mamba search xspec -c $HEASARC_CHANNEL --json | get_latest_pkg)
          url=$(echo $xspec | jq -r '.url')
          fn=$(echo $xspec | jq -r '.fn')
          version=$(echo $xspec | jq -r '.version')
          build=$(echo $xspec | jq -r '.build_string')
          echo "url=$url" >> "$GITHUB_OUTPUT"
          echo "fn=$fn" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "build=$build" >> "$GITHUB_OUTPUT"

          xspec_data=$(mamba search xspec-data -c $HEASARC_CHANNEL --json | get_latest_pkg)
          data_url=$(echo $xspec_data | jq -r '.url')
          data_fn=$(echo $xspec_data | jq -r '.fn')
          echo "data-url=$data_url" >> "$GITHUB_OUTPUT"
          echo "data-fn=$data_fn" >> "$GITHUB_OUTPUT"

          echo "VERSION=$version" >> "$GITHUB_ENV"
          echo "BUILD=$build" >> "$GITHUB_ENV"
          echo "FN=$fn" >> "$GITHUB_ENV"
          echo "URL=$url" >> "$GITHUB_ENV"
          echo "DATA_FN=$data_fn" >> "$GITHUB_ENV"
          echo "DATA_URL=$data_url" >> "$GITHUB_ENV"

      - name: Download Xspec ${{ env.VERSION }} (Build ${{ env.BUILD }})
        run: |
          aria2c -x 16 -s 16 --show-console-readout=true "$URL"
          if [ ! -f "$FN" ]; then
            echo "Failed to download from $URL"
            exit 1  # Exit with a non-zero status to fail the job
          fi

          aria2c -x 16 -s 16 --show-console-readout=true "$DATA_URL"
          if [ ! -f "$DATA_FN" ]; then
            echo "Failed to download from $DATA_URL"
            exit 1  # Exit with a non-zero status to fail the job
          fi

      - name: Upload Xspec Package as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.FN }}
          path: ${{ env.FN }}
          retention-days: 1

      - name: Upload Xspec Model Data Package as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.DATA_FN }}
          path: ${{ env.DATA_FN }}
          retention-days: 1

  tests:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    needs: download
    strategy:
      matrix:
        include:
          - name: Tests (Linux-x86, Python 3.9, Xspec ${{ needs.download.outputs.version }})
            os: "ubuntu-latest"
            python-version: 3.9

          - name: Tests (Linux-x86, Python 3.10, Xspec ${{ needs.download.outputs.version }})
            os: "ubuntu-latest"
            python-version: 3.10

          - name: Tests (Linux-x86, Python 3.11, Xspec ${{ needs.download.outputs.version }})
            os: "ubuntu-latest"
            python-version: 3.11

          - name: Tests (Linux-x86, Python 3.12, Xspec ${{ needs.download.outputs.version }})
            os: "ubuntu-latest"
            python-version: 3.12

          - name: Tests (Linux-arm64, Python 3.12, Xspec ${{ needs.download.outputs.version }})
            os: "ubuntu-24.04-arm"
            python-version: 3.12

          - name: Tests (macOS-arm64, Python 3.12, Xspec ${{ needs.download.outputs.version }})
            os: "macos-latest"
            python-version: 3.12
    env:
      PYTHON_VERSION: ${{ matrix.python-version }}
      XSPEC_FN: ${{ needs.download.outputs.fn }}
      XSPEC_DATA_FN: ${{ needs.download.outputs.data-fn }}
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Xspec Package
        uses: actions/download-artifact@v4

      - name: Install Miniforge
        uses: conda-incubator/setup-miniconda@v3
        with:
          activate-environment: test
          miniforge-version: latest
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Setup Conda Env
        run: |
          mamba create -y -n test python=$PYTHON_VERSION

      - name: Install Xspec
        run: |
          mamba install $XSPEC_FN $XSPEC_DATA_FN -c local

      - name: Install Python Dependencies
        run: |
          pip install ".[test]" --verbose

      - name: Run Tests
        run: |
          pytest

      - name: Upload Coverage Results
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true
          verbose: true

  build:
    name: Build Source Distribution
    runs-on: ubuntu-latest
    needs: download
    env:
      XSPEC_FN: ${{ needs.download.outputs.fn }}
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Xspec Package
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.XSPEC_FN }}

      - name: Install Miniforge
        uses: conda-incubator/setup-miniconda@v3
        with:
          activate-environment: build
          miniforge-version: latest
          python-version: 3.11

      - name: Setup Conda Env
        run: |
          mamba create -y -n build python=3.11

      - name: Install Xspec
        run: |
          mamba install $XSPEC_FN -c local

      - name: Install Python Dependencies
        run: |
          pip install -U build twine

      - name: Build and Check Source Distribution
        run: |
          python -m build --sdist .
          python -m twine check --strict dist/*

      - name: Upload Source Distribution as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: xspex
          path: dist/*

  publish:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    needs: [tests, build]
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    environment:
      name: PyPI
      url: https://pypi.org/p/xspex
    permissions:
      id-token: write
    steps:
      - name: Download Source Distribution
        uses: actions/download-artifact@v4
        with:
          name: xspex
          path: dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@v1.12.4
